# 运行问题修复与云端同步

本次代码已完成本地修复；云端配置和函数尚未部署，不能仅靠微信开发者工具重新编译使所有修复生效。请按下面的顺序同步，期间暂停旧版客户端使用，避免权限规则与新旧客户端混用。

## 已修复

- 积分商城、提现记录、骑手中心改为 Vue 2 写法，消除 `getCurrentInstance/ref is not a function`。
- 移除打开订单页时自动删除固定时间段订单的代码和对应可执行清理语句。
- 统一两端订单结构，商品 ID 改为字符串，补充隐藏标记、请求标识、预约配送及积分记录字段。
- 补齐店铺费用字段及两端的 `user_coupons` 表结构。
- 用户订单、优惠券、头像和积分改为通过 `user-service` 云函数处理，身份来自微信登录签发的随机会话令牌；不接受客户端自报 OpenID 作为授权凭据。
- 下单由云端读取菜品和费用配置重新计价，校验优惠券，使用事务保存订单、核销券和增加积分。请求标识避免重试重复下单。
- 取消待接单订单会返还关联券并撤回此次订单赠送的积分。用户删除记录仅设置个人隐藏标记，不删除商家订单。
- 订单、用户、菜单的直接写入权限收紧；商家使用 uni-admin 的 admin 账号管理。上一轮把菜品、分类的 `permission:false` 直接判断为管理员不可用不准确：官方 admin 角色不受这些规则限制，因此这两张表没有开放普通用户写入。
- 新登录头像先上传云存储，避免保存临时地址。地址页补齐新增、编辑、选择和删除，并按用户分别保存在当前设备。
- 统一两端桌号云对象，后台新增、编辑统一调用 `saveTable`；移除可公开返回 AppSecret 和 access token 的方法，桌号写入验证管理员身份。
- 业务代码中的微信 AppSecret 改为环境变量；PushPlus 改为 HTTPS，通知接口验证内部令牌并读取已保存订单。
- 清理无效旧路由，菜单加载失败时不返回可下单的静态测试菜品。

## 云端同步顺序

1. 在微信公众平台重置已提交过的 AppSecret。代码文件去掉密钥并不会清除 Git 历史，也不会使旧密钥失效。不要把新密钥发在聊天或提交到 Git。
2. 确认用户端和后台端连接同一个 uniCloud 服务空间。
3. 在云函数配置中设置环境变量：
   - `wx-login`：`WECHAT_APP_SECRET` 为新密钥。
   - `zhuohao`：`WECHAT_APP_SECRET` 为同一个新密钥；`WECHAT_APP_ID` 可选，默认沿用当前项目 AppID；`WECHAT_CODE_ENV` 默认为 `trial`（体验版），正式运营时设为 `release`。
   - `user-service` 与 `push-order-notification`：配置相同的 `ORDER_NOTIFICATION_TOKEN`，使用独立随机字符串。仅用于云函数之间的通知调用，不要填微信 AppSecret。不配置时订单仍可创建，但不会发送接单通知。
4. 从后台端上传 `uni-id-common` 及其依赖 `uni-config-center`（保留原有 uni-id 配置），然后上传后台端 `zhuohao` 云对象。用户端镜像依赖仓库中的后台端公共模块，推荐以后始终从后台端发布这个云对象。
5. 从用户端上传 `wx-login`、新增的 `user-service`、`push-order-notification` 云函数。
6. 上传数据库结构：后台端的 `order`、`wx_users`、`user_coupons`、`store_settings`、`my_menus`。这三份共有表（order/wx_users/user_coupons）已在两端保持一致。这里只上传 schema，不初始化、不清空已有数据。
7. 重新构建用户端微信小程序并在开发者工具编译。旧登录缓存没有新的会话令牌，请退出后重新登录一次。

更新必须同时包含云函数、数据库结构和客户端。先关闭直接访问权限而未上传 `user-service`，旧客户端会无法读取个人数据；这是需要协调发布的变更。

## 验证

- 本地回归：在仓库根目录执行 `node tests/ordering-regression.cjs`。
- 覆盖无效登录、跨用户读写拒绝、重复领券、篡改金额、失败回滚、重复下单幂等、取消返券、积分不可由客户端修改、订单软删除、三个 Vue 2 页面的初始化与事件。
- 用户端微信正式构建、后台 H5 正式构建通过；完整云端事务与微信登录仍需在上述部署后联调，模拟测试不能代替实际云空间验证。
- 部署后实际验证：登录 → 保存地址 → 领取新人券 → 满足门槛下单 → 后台接单；另建待接单订单验证取消、返券和隐藏记录。不要用真实付款验证，本项目仍未接入微信支付及退款接口。

## 仍需明确的功能边界

- 积分推广、提现记录和骑手中心仍含原有演示数据；本次修复其运行兼容性，没有接入真实提现或骑手调度。
- 用户个人列表最多读取最近 1000 条记录，地址只保存在当前设备；历史无 user_id 的测试订单仅在商家后台管理。
- AppSecret 的轮换、云端环境变量配置和部署不能通过本地构建证明完成。

参考：[DCloud 数据库权限说明](https://doc.dcloud.net.cn/uniCloud/schema.html)、[DCloud 事务说明](https://doc.dcloud.net.cn/uniCloud/cf-database?id=start-transaction)。
