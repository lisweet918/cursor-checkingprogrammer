# 扫码点餐小程序 — 程序架构

本文说明仓库的分层、模块划分、数据流和鉴权边界，便于后续改功能和排障。部署与操作步骤见 [应用使用文档.md](./应用使用文档.md)；云端同步与安全修复见 [修复与云端同步说明.md](./修复与云端同步说明.md)。

## 1. 系统全貌

项目是一套 **扫码堂食 + 外卖点餐** 应用，两个独立 uni-app 前端共享同一个 uniCloud（支付宝云）服务空间：

| 端 | 目录 | 运行形态 | 框架 |
| --- | --- | --- | --- |
| 用户端 | `点餐小程序的用户端/` | 微信小程序 | Vue 2 + uni-app + uniCloud |
| 管理后台 | `点餐小程序的后台端/` | 浏览器 H5（`/admin/`） | Vue 2 + uni-admin + uniCloud |

两端 **不共用登录体系**：顾客走微信 `jscode2session` + 自研会话令牌；商家走 uni-id 用户名密码。共享的是数据库、云存储和部分云对象。

```text
┌──────────────────────┐     ┌──────────────────────────┐
│ 微信小程序（用户端）   │     │ uni-admin H5（后台端）     │
│ Vuex + 本地购物车     │     │ uni-id + 侧栏业务菜单      │
│ userDatabase 代理     │     │ unicloud-db 直连业务表     │
└──────────┬───────────┘     └────────────┬─────────────┘
           │ callFunction / JQL            │ clientDB / 云对象
           ▼                               ▼
┌───────────────────────────────────────────────────────┐
│              uniCloud 同一服务空间                      │
│  wx-login  user-service  push-order-notification       │
│  zhuohao 云对象   uni-id-co   云存储                    │
│  业务库：order / product / category / wx_users / …     │
└───────────────────────────────────────────────────────┘
```

当前 **未接入微信支付**。用户端结算是「确认下单」，订单直接进入 `status = 0`（待接单）。

## 2. 仓库结构

```text
/
├── 点餐小程序的用户端/          微信小程序源码 + C 端云函数
├── 点餐小程序的后台端/          uni-admin 源码 + 后台云函数 + 完整 Schema
├── tests/                     本地回归（Node VM，不连真实云）
├── 应用使用文档.md
├── 修复与云端同步说明.md
├── 提交说明.md
└── 程序架构.md                 本文
```

不要把仓库根目录当成单个 uni-app 项目运行；HBuilderX 需分别导入两个子目录。`unpackage/` 是编译产物，不是业务入口。

## 3. 用户端架构

### 3.1 技术栈

- Vue 2（`manifest.json` → `vueVersion: 2`）
- uni-app 微信小程序，`mp-weixin.appid` 在 `manifest.json`
- UI：`uni_modules/vk-uview-ui`，自绘玻璃态 Tab（`components/glass-tabbar`）覆盖原生 tabBar
- 状态：Vuex（仅点餐上下文）+ `uni.storage`（会话、购物车、地址）
- Sass：`dart-sass`
- npm：`dayjs`、`z-paging`

### 3.2 页面与分包

主包 Tab：首页 / 订单 / 我的；登录与邀请页也在主包。业务重页放分包，首页预加载三个分包。

| 分包 | 路径 | 职责 |
| --- | --- | --- |
| 主包 | `pages/home` | 扫码入桌、堂食/外卖入口 |
| 主包 | `pages/order` | 订单列表、取消、再来一单 |
| 主包 | `pages/my` | 会员信息、动态菜单、退出 |
| 主包 | `pages/login` | 微信登录 |
| `subpackageHome` | `pointSingle`、`setTlement`、`pointsMall` | 点餐、结算、备注、积分商城占位 |
| `subpackageOrder` | `order-detail`、`coupon-detail` | 订单详情 |
| `subpackageMy` | 地址 / 资料 / 优惠券 / 余额 / 骑手 | 个人中心子页 |

**核心闭环**：扫桌码 → 首页选人数 → 点餐页加购 → 结算页确认 → `user-service.createOrder` → 订单 Tab。

以下页面目前是 UI 或演示数据，未接入真实履约：积分商城、提现记录、余额充值、骑手中心、好友邀请、券码订单 Tab。

### 3.3 状态分层

**Vuex**（`store/index.js`，仅内存，不持久化）：

| 字段 | 含义 |
| --- | --- |
| `orderType` | `takein` / `takeout` |
| `tableInfo` | 桌号、就餐人数 |
| `addressInfo` | 当前外卖地址 |
| `remark` | 订单备注 |

**本地存储**：

| Key | 内容 | 是否上云 |
| --- | --- | --- |
| `sessionToken` | 64 位 hex 会话令牌 | 仅发往 `user-service` |
| `userInfo` | 公开用户字段 | 登录时由云函数下发 |
| `cart` | 购物车行 | 否 |
| `addresses:{openid\|guest}` | 收货地址列表 | 否，按设备隔离 |

**服务端权威数据**：订单、积分、优惠券、会员档案。金额、库存适用性、券核销均在 `user-service` 内重算，不信任客户端提交的总价。

### 3.4 访问云端的三条路径

```text
页面
 ├─ $api('menu')           → category / product 只读 JQL + getTempFileURL
 ├─ uniCloud.database()    → store_settings / menu_banner / my_menus 只读
 ├─ wx-login 云函数        → 换 sessionToken
 └─ userDatabase()         → 把 order / wx_users / user_coupons
                              伪装成 collection API，实际 callFunction('user-service')
```

`common/user-api.js` 的 `userDatabase()` 是关键适配层：页面仍写 `db.collection('order').add(...)`，底层按集合映射到：

| 客户端调用 | `user-service` action |
| --- | --- |
| `.get()` / `.count()` | `read` |
| `order.add` | `createOrder` |
| `user_coupons.add` | `claimCoupon` |
| `wx_users.update({ avatar })` | `updateAvatar` |
| `order.update({ status: 2 })` | `updateOrder`（仅允许取消） |
| `order.remove()` | `hideOrder`（软删除） |

`$api('promotion')` / `$api('order')` 仍返回 `common/request` 下的静态 mock，只给积分商城等占位页使用。菜单云端为空时返回 `[]`，不再用测试菜品兜底。

### 3.5 关键组件

| 组件 | 作用 |
| --- | --- |
| `glass-tabbar` | 自定义底部导航 |
| `cartbar` / `cart-popup` | 购物车底栏与明细 |
| `actions` / `product-modal` | 加减购、规格配料 |
| `search` | 菜单内搜索 |
| `yier-art` / `yier-feedback` | IP 形象与空态 |
| `hbxw-timepicker` | 外卖预约送达 |

饮品规格预处理在 `common/product-preferences.js`。

## 4. 管理后台架构

基于官方 **uni-admin 2.5.13** 模板，H5 路由 `base: /admin/`。

### 4.1 壳层

- `windows/topWindow.vue`：顶栏、主题、语言、当前用户
- `windows/leftWindow.vue`：侧栏菜单
- `js_sdk/uni-admin/plugin.js`：权限、拦截器、请求、uni-id store
- Vuex 模块：`app`（菜单/主题）、`user`（管理员资料）、`error`（H5 错误队列）

登录守卫由 `pages.json` 的 `uniIdRouter` 完成：除登录/注册/找回密码外均需 uni-id token。

### 4.2 菜单来源

1. **动态菜单**：`opendb-admin-menus`（系统管理、uni 统计等），按当前用户 `permission` 过滤。
2. **静态菜单**：`admin.config.js` → `sideBar.staticMenu`，挂载点餐业务：订单、菜品、轮播、店铺设置、我的页菜单、会员。

桌号页 `pages/zhuohao/list|add|edit` 已在 `pages.json` 注册，**尚未写入 staticMenu**，需直接打开路由。

### 4.3 业务 CRUD 方式

| 模块 | 页面 | 写数据方式 |
| --- | --- | --- |
| 订单 | `pages/order/list` | 客户端 `order.update`（改 `status` / `is_show`） |
| 分类 / 菜品 | `pages/menu/**` | 客户端 DB |
| 首页 / 点餐轮播 | `pages/banner`、`pages/menu_banner` | 客户端 DB |
| 店铺设置 | `pages/store/settings` | 客户端 DB `store_settings` |
| 我的页入口 | `pages/my_menus` | 客户端 DB |
| 会员积分 | `pages/wx_users/list` | 客户端 DB 改 `points` |
| 桌号 + 小程序码 | `pages/zhuohao` | **云对象** `zhuohao.saveTable`；列表删除走 DB |

管理员权限依赖 uni-id 的 `admin` 角色。仓库 Schema 对若干表把客户端写设为 `false`；后台页面仍走 clientDB，**云端实际权限需与已登录管理员策略对齐**（uni-admin 的 admin 角色通常不受 schema `permission: false` 限制）。

系统能力（账号、角色、权限、应用、uni 统计、升级中心）沿用模板，不是点餐业务核心。

## 5. 云函数与云对象

两端都有 `uniCloud-alipay/`。**C 端业务函数在用户端目录上传**；**桌号云对象与完整 Schema 以后台端为准**，需部署到同一服务空间。

### 5.1 用户端云函数

| 名称 | 类型 | 职责 |
| --- | --- | --- |
| `wx-login` | 云函数 | `code` 换 openid，签发 7 天会话 |
| `user-service` | 云函数 | 顾客侧私有数据网关（订单/券/头像/取消） |
| `push-order-notification` | 云函数 | 下单后读库，经 PushPlus 通知商家 |
| `zhuohao` | 云对象 | 与后台镜像；用户端运行时不调用 |

### 5.2 `wx-login`

1. 用 `WECHAT_APP_ID` / `WECHAT_APP_SECRET` 调微信 `jscode2session`。
2. 按 `openid` upsert `wx_users`。
3. 生成 32 字节随机 token（64 hex），库内只存 `session_hash = sha256(token)` 和 `session_expires`。
4. 返回 `{ token, user }`（公开字段，不含 hash）。

客户端把 token 存为 `sessionToken`。过期或格式非法时 `user-service` 返回 `UNAUTHORIZED`，客户端清空本地会话。

密钥必须配在云函数环境变量。代码里的 fallback 仅便于本地调试，不应出现在生产或公开仓库。

### 5.3 `user-service`

所有 action 先 `authenticate(token)`。

| action | 行为 |
| --- | --- |
| `read` | 读当前用户的 `wx_users` / `order` / `user_coupons`；订单过滤 `is_show===false` 与 `user_hidden` |
| `claimCoupon` | 每人一张新人券（满 20 减 5，30 天）；确定性 `_id` 防并发重复领 |
| `createOrder` | 校验菜品上架与堂食/外卖标记、**服务端按分重算价格**、起送与券门槛、事务写单 + 核销券 + 积分 +10；`request_id` 幂等 |
| `updateAvatar` | 仅允许 `cloud://` 或 `https://` |
| `updateOrder` | 仅待接单可改为 `status=2`；回滚本次积分并返还优惠券 |
| `hideOrder` | 已取消/已完成订单设 `user_hidden`，商家记录保留 |

`createOrder` 成功后内部调用 `push-order-notification`，传入 `orderId` 与 `ORDER_NOTIFICATION_TOKEN`，**不把订单明细交给通知函数**。

### 5.4 `zhuohao` 云对象

| 方法 | 鉴权 | 作用 |
| --- | --- | --- |
| `getDetailByTableNumber` | 公开 | 按桌号查记录（用户端当前未调用） |
| `createMiniCode` | uni-id `admin` | 调微信 `getwxacodeunlimit`，上传云存储 |
| `saveTable` | admin | 生成码后 add/update：`tableNumber`、`orderQrCode`、`scene` |
| `list` | admin | 按创建时间倒序 |

小程序码 `page` 指向 `pages/home/home`，`scene` 为 `tableNumber={桌号}`。`WECHAT_CODE_ENV` 默认 `trial`（体验版），正式运营改为 `release`。

用户扫码后首页只解析 scene，**不强制校验** `zhuohao` 表是否存在该桌号。

### 5.5 后台其它云能力

`uni-id-co`（管理员登录与 RBAC）、`ext-storage-co`（上传参数）、`uni-stat-*`、`uni-upgrade-center`、`uni-portal`、`uni-sms-co`、`check-users`（调试读 `wx_users`）。点餐主链路不依赖统计与短信。

## 6. 数据模型

权威 Schema 在 `点餐小程序的后台端/uniCloud-alipay/database/`。用户端仅同步 `order`、`wx_users`、`user_coupons`、`zhuohao`。

### 6.1 业务集合

| 集合 | 用途 | 用户端 | 后台端 |
| --- | --- | --- | --- |
| `category` | 菜品分类 | 只读 JQL | 客户端 CRUD |
| `product` | 菜品、价格、堂食/外卖标记 | 只读 JQL；下单时云函数再读 | 客户端 CRUD |
| `banner` | 首页轮播（后台已维护） | **首页目前用本地静态图** | 客户端 CRUD |
| `menu_banner` | 点餐页轮播 | 只读 | 客户端 CRUD |
| `store_settings` | 店名、营业时间、费用、PushPlus Token | 只读费用字段 | 客户端读写 |
| `my_menus` | 「我的」动态入口 | 只读 | 客户端 CRUD |
| `zhuohao` | 桌号与小程序码 | 不直连 | 云对象 + 列表删除 |
| `wx_users` | 微信会员、积分、会话哈希 | 仅经 `wx-login` / `user-service` | 列表改积分 |
| `order` | 堂食/外卖订单 | 仅经 `user-service` | 改状态与显隐 |
| `user_coupons` | 用户券 | 仅经 `user-service` | 无独立管理页 |

管理员身份在 `uni-id-users` / `uni-id-roles` / `uni-id-permissions`，与 `wx_users` 无关。

### 6.2 订单约定

- `type`：`takein` | `takeout`
- `status`：`0` 待接单 → `1` 已接单 → `3` 已完成；`2` 已取消/页面文案为「已退款」（**无真实退款接口**）
- 费用字段：`goods_amount`、`packing_fee`、`delivery_fee`、`coupon_deduct`、`price`
- 堂食：`tableNumber`、`diningCount`；外卖：姓名电话地址、`delivery_type`、`reservation_time`
- `is_show`：商家隐藏；`user_hidden`：用户删除自己的列表项
- `request_id`：客户端幂等键；订单 `_id = sha256(openid:request_id).slice(0,24)`
- `points_awarded`：本次下单赠送积分，取消时扣回

### 6.3 Schema 权限意图

`order` / `wx_users` / `user_coupons` / `zhuohao` 对客户端直连为全禁，逼顾客走云函数。`category` / `product` / `my_menus` 对匿名只读。`store_settings` 可读；`pushplus_token` 字段本身不可被客户端读出。

## 7. 主链路数据流

### 7.1 扫码堂食

```text
后台 saveTable → 微信码 scene=tableNumber=N
        ↓ 顾客扫码
首页解析 scene → Vuex tableInfo → 选人数
        ↓
点餐页 $api('menu') + 本地 cart
        ↓
结算页 展示桌号/人数 → createOrder(type=takein)
        ↓
事务写 order / 核销券 / points+10 → PushPlus
```

### 7.2 外卖

首页或点餐页切 `takeout` → 结算校验本地地址与起送金额 → `createOrder` 在云端再算打包费、配送费（满额免配）和券抵扣。

### 7.3 登录与「我的」

「我的」未登录跳转 `pages/login` → `uni.login` + 昵称头像（头像先 `uploadFile`）→ `wx-login`。动态菜单来自 `my_menus`。优惠券列表与领取走 `user-service`。退出只清本地 token / cart / 地址，不吊销服务端 hash（7 天后自然过期）。

### 7.4 商家接单

后台订单列表读共享 `order` 表，把 `status` 改为 1 或 3，或把 `is_show` 设为 false。用户列表随即看不到被隐藏的单。接单通知依赖店铺里配置的 PushPlus Token，以及两边云函数相同的 `ORDER_NOTIFICATION_TOKEN`。

## 8. 双鉴权模型

```text
顾客                          商家
uni.login code                用户名密码
    ↓                              ↓
wx-login                      uni-id-co
sessionToken (64 hex)         uni_id_token
wx_users.session_hash         uni-id-users.role
    ↓                              ↓
user-service.authenticate     zhuohao._before 要求 admin
```

两边 token **不能互换**。顾客接口不接受客户端自报 openid；必须以令牌在库中命中未过期的 `session_hash`。

## 9. 测试

`/workspace/tests` 用 Node 加载源码做回归，不连接真实 uniCloud：

| 文件 | 覆盖 |
| --- | --- |
| `ordering-regression.cjs` | 登录伪造、跨用户拒绝、领券唯一、改价拦截、下单幂等、取消返券积分、软删除、地址本地隔离 |
| `product-preferences.cjs` | 规格默认值与购物车分行 |
| `glass-navigation.cjs` | 自定义 Tab 与 `pages.json` 一致性 |
| `glass-motion.cjs` | 玻璃态触摸 mixin |

仓库根目录执行：`node tests/ordering-regression.cjs`。微信登录、云事务、桌码生成仍需云端联调。

## 10. 架构边界与注意点

1. **购物车和地址只在当前设备**。换机或清缓存会丢地址；历史无 `user_id` 的测试单只在后台可见。
2. **打包费**按店铺配置计入订单；文档曾说明费用面向外卖，实现上堂食也会带 `packing_fee` 字段（当前 `createOrder` 对堂食同样读取该配置）。
3. **首页 `banner` 表与页面脱节**：后台可配首页轮播，用户端首页仍用静态资源；点餐页已读 `menu_banner`。
4. **桌号管理未进侧栏**，容易漏配。
5. **`zhuohao` 在两端各有一份源码**，上传时必须同步，避免后台改了用户端空间仍是旧对象。
6. 积分商城、提现、骑手、余额、微信支付均为未完成能力，不要按生产履约理解。
7. 修改表字段时同时改：两端 Schema、用户端 `user-service`、后台表单、本文与使用文档。
