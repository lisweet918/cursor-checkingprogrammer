# 扫码微信点餐小程序应用使用文档

## 1. 应用简介

本项目是一套基于 uni-app、Vue 2、uniCloud 和 uni-admin 的扫码点餐应用，包含两个独立前端：

- 用户端：微信小程序，提供扫码入桌、堂食/外卖点餐、购物车、结算、优惠券、订单和会员功能。
- 管理后台：uni-admin 管理端，提供桌号、菜品、分类、轮播图、订单、店铺配置、会员和权限管理。

两个前端需要绑定同一个 uniCloud 服务空间，才能共享店铺、商品、订单和会员数据。

## 2. 项目目录

```text
扫码微信点餐小程序_副本/
├── 点餐小程序的用户端/       # 微信小程序用户端
│   ├── pages/                # 首页、订单、我的、登录
│   ├── subpackageHome/       # 点餐、结算、积分商城
│   ├── subpackageOrder/      # 订单详情
│   ├── subpackageMy/         # 地址、资料、优惠券等
│   ├── store/                # 订单类型、地址、备注、桌号状态
│   └── uniCloud-alipay/      # 云函数和数据库 Schema
├── 点餐小程序的后台端/       # uni-admin 管理后台
│   ├── pages/                # 业务管理页面
│   └── uniCloud-alipay/      # 后台云函数和数据库 Schema
├── 提交说明.md
└── 应用使用文档.md
```

`unpackage/` 是 HBuilderX 运行或发行时生成的目录，不是日常修改业务代码的入口。

## 3. 运行环境

建议准备：

1. HBuilderX，版本满足后台项目 `HBuilderX ^3.6.0` 要求。
2. 微信开发者工具。
3. DCloud 账号及可用的 uniCloud 阿里云服务空间。
4. 微信小程序 AppID 和对应开发权限。
5. Node.js 与 npm，用于安装用户端依赖。

用户端依赖包括：

- `dayjs`
- `z-paging`

## 4. 首次部署

### 4.1 导入项目

1. 使用 HBuilderX 分别导入：
   - `点餐小程序的用户端`
   - `点餐小程序的后台端`
2. 不要将仓库最外层目录直接当成单个 uni-app 项目运行。

### 4.2 安装用户端依赖

在用户端目录执行：

```bash
npm install
```

如果项目已包含完整 `node_modules`，仍建议按锁定文件和实际运行环境重新安装依赖。

### 4.3 绑定 uniCloud 服务空间

1. 在 HBuilderX 中登录 DCloud 账号。
2. 为用户端和管理后台选择同一个 uniCloud 阿里云服务空间。
3. 关联 `uniCloud-alipay` 目录。
4. 上传并部署项目所需云函数，至少包括：
   - `wx-login`
   - `zhuohao`
   - `push-order-notification`（需要接单推送时）
5. 上传数据库 Schema，并确认业务集合已创建。

### 4.4 核心数据库集合

| 集合 | 用途 |
| --- | --- |
| `category` | 菜品分类 |
| `product` | 菜品信息、价格和可售模式 |
| `banner` | 首页轮播图 |
| `menu_banner` | 点餐页轮播图 |
| `zhuohao` | 桌号、桌码及场景参数 |
| `order` | 堂食和外卖订单 |
| `wx_users` | 微信用户和会员积分 |
| `store_settings` | 店铺名称、营业时间、通知与费用配置 |
| `my_menus` | “我的”页面动态菜单 |
| `user_coupons` | 用户优惠券；当前需补充 Schema 和权限 |

首次部署前请阅读 `提交说明.md` 中的数据库配套要求，补齐费用、就餐人数和优惠券字段。

### 4.5 配置微信小程序

1. 在用户端 `manifest.json` 的微信小程序配置中填写实际 AppID。
2. 云函数 `zhuohao` 生成小程序码时也需要可用的微信小程序凭证和发布页面路径。
3. 在微信公众平台配置合法域名、隐私协议和所需权限。
4. HBuilderX 选择“运行到小程序模拟器 → 微信开发者工具”。

不要把生产密钥、AppSecret 或访问令牌写入公开仓库。

### 4.6 启动管理后台

管理后台基于 uni-admin，可在 HBuilderX 中运行到浏览器。首次使用时：

1. 创建超级管理员账号。
2. 登录后配置角色、权限和菜单。
3. 确认业务菜单能够进入桌号、订单、菜品、分类、轮播图、店铺设置和会员管理页面。

## 5. 开业前配置顺序

建议管理员按以下顺序准备数据：

1. 店铺设置：填写店铺名称、营业时间、通知 Token 和外卖费用。
2. 分类管理：创建早餐、主食、饮品等分类并设置排序。
3. 菜品管理：填写名称、分类、售价、图片、规格、是否上架及适用点餐模式。
4. 轮播图管理：配置首页和点餐页活动图片。
5. 桌号管理：创建桌号并生成对应小程序码，下载后张贴到餐桌。
6. 我的页面菜单：根据实际业务配置地址、余额、积分、骑手中心等入口。
7. 使用测试账号完成一笔堂食订单和一笔外卖订单。

## 6. 顾客使用流程

### 6.1 登录与个人资料

1. 进入“我的”。
2. 点击头像或登录提示进入登录页。
3. 登录后可以查看积分、优惠券数量，并进入个人资料设置。
4. 外卖下单前需要在收货地址中维护姓名、电话、地址和门牌号。

### 6.2 扫码堂食

1. 顾客使用微信扫描餐桌小程序码。
2. 首页从小程序场景参数读取 `tableNumber`。
3. 顾客选择就餐人数。
4. 选择堂食并进入点餐页。
5. 将菜品加入购物车，填写备注后进入结算页。
6. 确认桌号、人数、商品和金额后提交订单。
7. 订单会保存桌号和就餐人数，管理员可在订单详情中查看。

如果桌码没有携带有效桌号，系统不会显示桌号卡片，应检查桌码是否由当前小程序和 `zhuohao` 云函数生成。

### 6.3 外卖点餐

1. 在首页或点餐页切换为外卖模式。
2. 选择菜品并进入结算页。
3. 选择收货地址、配送时间并填写备注。
4. 系统按后台配置计算：
   - 商品小计
   - 打包费
   - 配送费
   - 优惠券抵扣
5. 商品小计达到免配送费门槛后，配送费显示为免费。
6. 商品小计未达到起送金额时，确认按钮不可用。
7. 确认实付金额后提交订单。

### 6.4 优惠券

1. 进入“我的 → 优惠券”。
2. 点击新人券区域领取“满 20 减 5”优惠券。
3. 每位登录用户只可领取一次。
4. 结算页会显示满足门槛且未过期的优惠券。
5. 选中优惠券后，实付金额自动扣减。
6. 下单成功后优惠券进入“已使用”列表。

### 6.5 查看和管理订单

1. 进入底部“订单”标签。
2. 在自取/堂食和外卖分类中查看订单。
3. 下拉页面可刷新订单状态。
4. 点击订单进入详情，查看商品、费用、地址、桌号和备注。
5. 待接单订单可取消。
6. 点击“再来一单”可将历史商品放入购物车并重新结算。

“再来一单”会带入历史商品价格，提交前应核对当前菜单和价格。

## 7. 管理员使用流程

### 7.1 店铺设置

在“店铺设置”中维护：

- 店铺名称
- 营业时间
- PushPlus 接单通知 Token
- 打包费
- 配送费
- 免配送费门槛
- 外卖起送金额

金额留空、填写负数或非法值时会按 0 保存；金额最多保留两位小数。

### 7.2 菜品与分类

1. 先创建分类。
2. 新增菜品并选择所属分类。
3. 设置价格、图片、排序、规格和上架状态。
4. 检查菜品是否适用于堂食或外卖模式。
5. 用户端没有符合当前模式的菜品时会显示空状态。

### 7.3 桌号与桌码

1. 进入桌号管理创建桌号。
2. 生成小程序码。
3. 下载二维码并进行实际扫码测试。
4. 确认首页显示的桌号与餐桌一致。
5. 不要在不同餐桌重复使用同一个桌码。

### 7.4 订单处理

订单主状态目前定义为：

| 状态值 | 页面文案 | 建议业务含义 |
| ---: | --- | --- |
| 0 | 待接单 | 顾客已提交，等待商家确认 |
| 1 | 已接单 | 商家正在制作或配送 |
| 2 | 已退款 | 已取消或已退款，需统一实际业务定义 |
| 3 | 已完成 | 订单履约结束 |

管理员可以在订单详情中核对：

- 订单号和下单时间
- 堂食/外卖类型
- 桌号和就餐人数
- 联系人、电话和地址
- 商品明细与规格
- 商品小计、打包费、配送费、优惠券和总金额
- 顾客备注

### 7.5 会员与页面配置

- 会员管理用于查看微信用户和积分。
- 首页轮播图和点餐页轮播图分别维护。
- “我的页面菜单管理”可以控制个人中心动态入口、排序和显示状态。
- uni-admin 的用户、角色和权限应按员工职责分配，避免所有账号均使用超级管理员。

## 8. 数据与安全建议

1. 用户订单查询应限定 `user_id` 为当前登录用户。
2. 订单金额应在受信任的云函数中根据最新商品和店铺配置重新计算。
3. 优惠券领取、校验、订单创建和核销应使用云函数事务，防止重复领取或重复使用。
4. 管理后台更新订单和店铺配置需要管理员权限。
5. PushPlus Token、微信密钥等敏感信息应放入云函数配置或安全配置中心。
6. 发布前收紧数据库 Schema 权限，不要保留生产环境中的全量读写权限。
7. 定期备份 `order`、`wx_users`、`store_settings` 和 `user_coupons`。

## 9. 发布检查

### 用户端

- [ ] 首页、扫码、人数选择和点餐模式正常。
- [ ] 商品、分类、图片和规格显示正常。
- [ ] 堂食和外卖各完成一次完整下单。
- [ ] 地址、备注、费用、优惠券和总金额正确。
- [ ] 订单列表、详情、取消和再来一单正常。
- [ ] 登录、头像、积分和优惠券页面正常。

### 管理后台

- [ ] 管理员登录和权限正常。
- [ ] 店铺、分类、菜品、轮播图和桌号配置正常。
- [ ] 桌码可以打开正式版小程序并携带正确桌号。
- [ ] 新订单可见，状态操作和费用明细正确。
- [ ] 接单通知在配置 Token 后可以收到。

### 云端

- [ ] 用户端和后台连接同一服务空间。
- [ ] Schema、索引和云函数均已部署。
- [ ] 数据库权限已按正式环境收紧。
- [ ] 日志中没有登录、查询、下单或核销异常。

## 10. 常见问题

### 扫码后没有桌号

检查小程序码场景参数是否为 `tableNumber=实际桌号`，并确认二维码指向当前发布的小程序页面。

### 用户端没有菜品

检查菜品是否上架、是否属于有效分类，以及菜品是否支持当前堂食/外卖模式。

### 外卖无法提交

检查是否选择了完整地址，以及商品小计是否达到后台设置的起送金额。

### 优惠券页面加载失败

检查 `user_coupons` 集合、Schema、索引和权限是否已创建，并确认用户已登录且本地用户信息包含 OpenID。

### 后台看不到新订单

检查用户端和后台是否绑定同一 uniCloud 服务空间，以及订单集合权限和查询条件是否正确。

### Sass 编译报错

用户端已配置为 `dart-sass`。请使用兼容的 HBuilderX 版本，并避免继续依赖仅支持 `node-sass` 的旧语法。

## 11. 维护约定

1. 业务源码修改在 `pages`、`subpackage*`、`components`、`common`、`store` 和 `uniCloud-alipay` 中进行。
2. 不直接修改 `unpackage/dist` 下的编译文件。
3. 每次修改数据字段时同步更新两端 Schema 和本文档。
4. 发布前分别测试堂食、外卖、未登录、无地址、无商品、优惠券过期和网络异常场景。
5. Git 提交中尽量排除 `.DS_Store`、本机编辑器配置、日志和无必要的编译产物。
