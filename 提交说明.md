# 提交说明

## 1. 提交概述

- 提交主题：订单体验、桌号信息、外卖费用、优惠券及使用文档
- 适用项目：扫码微信点餐小程序
- 涉及端：用户端、管理后台、uniCloud 数据库
- 文档日期：2026-08-08

本次工作区修改完善了堂食和外卖订单链路，增加费用配置、优惠券、订单快捷操作与空状态，并补充项目部署和使用说明。

## 2. 功能变更

### 2.1 管理后台

1. 店铺设置新增以下外卖费用字段：
   - 每单打包费 `packing_fee`
   - 基础配送费 `delivery_fee`
   - 免配送费门槛 `free_delivery_threshold`
   - 外卖起送金额 `min_order_amount`
2. 金额输入会转换为非负、最多两位小数的数值后写入 `store_settings`。
3. 订单详情新增：
   - 桌号与就餐人数
   - 商品小计
   - 打包费
   - 配送费
   - 优惠券抵扣

### 2.2 用户端点餐与结算

1. 堂食点餐页显示扫码获得的桌号和用户选择的就餐人数。
2. 当前点餐模式没有可售商品时显示空状态，并提供切换点餐模式入口。
3. 结算页显示堂食桌号和人数。
4. 外卖结算支持：
   - 打包费
   - 配送费
   - 满额免配送费
   - 起送金额校验
5. 结算页支持选择可用优惠券，并实时计算优惠金额和实付金额。
6. 订单新增保存以下字段：
   - `diningCount`
   - `goods_amount`
   - `packing_fee`
   - `delivery_fee`
   - `coupon_deduct`
   - `coupon_id`
7. 下单成功后记录优惠券使用状态、关联订单和使用时间。

### 2.3 用户订单

1. 订单列表新增下单时间和不同状态的视觉区分。
2. 支持下拉刷新订单。
3. 待接单订单增加“取消订单”操作。
4. “再来一单”会恢复历史订单商品并进入结算页。
5. 自取和外卖列表均增加无订单空状态。
6. 订单详情展示费用明细、桌号和就餐人数。

### 2.4 我的页面与优惠券

1. “我的”页面增加订单快捷入口。
2. 显示当前用户可用优惠券数量。
3. 新增“我的优惠券”页面，按可使用、已使用和已过期分类。
4. 支持每位登录用户领取一次“满 20 元减 5 元”的新人券，有效期为领取后 30 天。

### 2.5 工程配置与样式

1. 用户端 Sass 实现从 `node-sass` 切换为 `dart-sass`。
2. 用户端订单页开启下拉刷新。
3. 注册“我的优惠券”分包页面。
4. 新增结算费用、优惠券弹层、订单入口、状态、空页面和桌号标签等样式。

## 3. 主要文件

### 管理后台

- `点餐小程序的后台端/pages/store/settings.vue`
- `点餐小程序的后台端/pages/order/list.vue`

### 用户端

- `点餐小程序的用户端/manifest.json`
- `点餐小程序的用户端/pages.json`
- `点餐小程序的用户端/pages/my/my.vue`
- `点餐小程序的用户端/pages/order/order.vue`
- `点餐小程序的用户端/subpackageHome/pointSingle/point-single.vue`
- `点餐小程序的用户端/subpackageHome/setTlement/pay.vue`
- `点餐小程序的用户端/subpackageOrder/order/order-detail.vue`
- `点餐小程序的用户端/subpackageMy/myCoupon/coupon-list.vue`
- 对应的 SCSS 样式文件

## 4. 数据库配套要求

发布前需要确认前后端绑定同一个 uniCloud 服务空间，并完成以下数据结构调整。

### 4.1 `store_settings`

增加四个数值字段：

| 字段 | 类型 | 默认值 | 说明 |
| --- | --- | ---: | --- |
| `packing_fee` | double | 0 | 每单打包费 |
| `delivery_fee` | double | 0 | 基础配送费 |
| `free_delivery_threshold` | double | 0 | 达到此商品金额免配送费；0 表示不启用 |
| `min_order_amount` | double | 0 | 外卖起送商品金额；0 表示不限制 |

### 4.2 `order`

增加以下字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `diningCount` | int | 堂食人数 |
| `goods_amount` | double | 商品小计 |
| `packing_fee` | double | 打包费 |
| `delivery_fee` | double | 实际配送费 |
| `coupon_deduct` | double | 优惠券抵扣金额 |
| `coupon_id` | string | 使用的用户优惠券记录 ID |

### 4.3 `user_coupons`

当前代码已使用该集合，但项目中尚未提供对应的 Schema 文件。至少应包含：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `user_id` | string | 用户 OpenID |
| `name` | string | 优惠券名称 |
| `threshold` | double | 使用门槛 |
| `amount` | double | 优惠金额 |
| `status` | int | 0=未使用，1=已使用 |
| `source` | string | 来源，如 `newbie` |
| `create_time` | timestamp | 领取时间 |
| `expire_time` | timestamp | 过期时间 |
| `used_order_id` | string | 使用订单 ID |
| `used_time` | timestamp | 使用时间 |

建议为 `user_id + source` 建立联合索引，并通过数据库权限或云函数保证用户只能读取和操作自己的优惠券。

## 5. 验收清单

### 管理后台

- [ ] 店铺费用配置可以保存并在刷新后正确回显。
- [ ] 订单详情能正确显示堂食人数和各项费用。
- [ ] 管理员可以按预期更新订单状态。

### 用户端

- [ ] 扫桌码后能识别桌号并选择就餐人数。
- [ ] 堂食订单能保存桌号和人数。
- [ ] 外卖费用、免配送费门槛和起送金额计算正确。
- [ ] 未达到起送金额时不能提交外卖订单。
- [ ] 新人券只能领取一次。
- [ ] 可用、已用和过期优惠券分类正确。
- [ ] 使用优惠券后订单金额和优惠券状态一致。
- [ ] 取消订单、再来一单、下拉刷新和空状态正常。
- [ ] 后台订单金额与用户端订单详情一致。

## 6. 已知注意事项

1. 当前“取消订单”会把订单状态设置为 `2`，现有状态定义将 `2` 显示为“已退款”；发布前应确认业务语义。
2. 店铺设置文案说明费用仅影响外卖，但当前打包费计算未区分堂食和外卖；发布前应确认收费规则。
3. 订单创建与优惠券核销是两次独立写入。核销失败时订单仍会创建，正式上线建议迁移到云函数事务中。
4. “再来一单”使用历史订单中的商品价格，正式下单前应考虑重新读取最新商品信息和售价。
5. `order` 现有 Schema 权限较宽，正式环境应限制用户只能创建和读取自己的订单，订单状态更新由受控云函数或管理员完成。
6. `.DS_Store`、`.vscode` 和 `unpackage/dist` 属于系统配置或构建产物，不应作为本次业务提交的主体。

## 7. 回滚说明

如需回滚，应同步回滚用户端、管理后台和云数据库配置。仅回滚界面代码而保留新费用字段不会影响旧订单读取，但旧版客户端不会展示新费用明细；若移除 `user_coupons` 集合，应同时关闭优惠券入口和结算页优惠券查询。
